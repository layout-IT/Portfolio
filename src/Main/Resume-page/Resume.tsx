import React from "react";
import {NavLink} from "react-router-dom";

export function Resume() {
    return (
        <div>
            this is a Page Resume
        </div>
    );
}

export default Resume;
